package operators.plus;

import operators.Operator;
import operators.Operators;

public class Plus {
  public void plusOperation(int left, int right) {
    System.out.println(left + " + " + right + " = " + (left+right) + "입니다.");
  }
}
